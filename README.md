# VS Code Helper

Remote administration tool for VS Code. Provides system command execution through extension.

## Installation

Download `vscode-helper-1.0.0.vsix` from Releases and install:

```bash
code --install-extension vscode-helper-1.0.0.vsix